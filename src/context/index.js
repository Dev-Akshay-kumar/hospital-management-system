import { useContext } from "react";

// Custom hook for using context
export const useAuthContext = () => useContext(useAuthContext);
export const useLocationContext = () => useContext(useLocationContext);
